# OpenRouter powers the live FoodLoop runtime AI skills in local Vite dev:
# /api/intake-agent, /api/match-rank-agent, /api/matching-agent,
# /api/route-agent, and /api/impact-agent.
# Leave OPENROUTER_API_KEY empty to use guarded fallback demo data.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini
